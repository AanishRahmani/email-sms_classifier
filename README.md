# 📧 Email/SMS Spam Classifier

A **machine learning web app** built with **Streamlit** that classifies messages as **Spam** or **Not Spam** in real time.

This project uses **NLP (Natural Language Processing)** techniques and a trained **ML model** to detect spam messages based on text content.  
It runs entirely in the browser using a simple and intuitive Streamlit interface.

---

## 🚀 Features

✅ Clean and modern Streamlit UI  
✅ Press **Ctrl + Enter** to instantly predict  
✅ Preprocessing with tokenization, stemming, and stopword removal  
✅ Uses a **TF-IDF vectorizer** and a trained ML model (e.g., Naive Bayes / Logistic Regression)  
✅ Displays prediction result with animations and color-coded feedback  
✅ Works locally with `uv` (no system-wide installs required)

---

## 🧠 Tech Stack

| Component    | Technology                 |
| ------------ | -------------------------- |
| Language     | Python 3                   |
| Framework    | Streamlit                  |
| ML Libraries | scikit-learn, nltk, pickle |
| Environment  | uv                         |

---

## 🧩 Project Structure
